import ReactDOM from 'react-dom/client';
// expprtng reactdom object from package.json object  
import './index.css';
import App from './App.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
